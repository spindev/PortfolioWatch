# No environment variables required for production.
#
# Market data is fetched server-side by scripts/fetch-finance-data.mjs during
# the GitHub Actions build and served as static JSON files from the same origin.
# See README.md for details.

